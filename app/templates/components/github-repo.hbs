
{{#link-to 'org.repo.issues' repo.name}}

  {{#if hasBlock}}
    {{yield}}
  {{else}}
    {{repo.name}}
  {{/if}}
{{/link-to}}

<span class="grey-text">
(
Forks: {{nice-number repo.forks_count}}
Watchers: {{nice-number repo.watchers_count}}
)
</span>